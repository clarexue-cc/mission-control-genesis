# P6 Evidence - Vault structure review

Date: 2026-04-29
Target: `http://127.0.0.1:3300/onboarding/customer/deploy?role=admin&tenant=media-intel-v1`
Tenant: `media-intel-v1`
Operator: `admin`

Checkpoint: P6 / OB-S4 remains paused for Clare confirmation.

Visible state:
- Page title: `new-tenant + Docker 部署`
- Tenant ID: `media-intel-v1`
- `confirmation-cc.md` found
- Container: `tenant-media-intel-v1-mock`
- Vault structure is visible as a tree
- Directories are blue and files are green
- Directory rows do not show P labels
- File rows show only the stage number (`P3`, `P4`, `P5`, `P6`, `P7`, `P9`)
- Only `P6` template files expose `查看`
- `查看` reads the real tenant vault path: `phase0/tenants/media-intel-v1/vault/...`

Current P6 interpretation:
- P6 creates the tenant vault skeleton under `phase0/tenants/media-intel-v1/vault`.
- P6 copies template documents from `phase0/templates/vault-template`.
- P6 does not perform customer-specific content optimization inside those template documents.
- Later generation is expected for SOUL/AGENTS (`P7`), skills (`P9`), and memory validation/update (`P13` or later).

Open discussion for 大管家:
- Whether the tenant vault or Obsidian shared vault should be the source of truth.
- How `phase0/tenants/<tenant>/vault` should map, mount, or sync to Obsidian.
- How OpenClaw agents should read/write shared memory without creating a second divergent copy.
- Hermes should remain an event/alert layer, not the source of truth for vault content.
- Template files need separate management: where templates live, who edits them, how versioning/release works, and whether customer-specific overrides are allowed before P6 copy.

Screenshot:
- `p6-vault-structure-final-20260429.png`
