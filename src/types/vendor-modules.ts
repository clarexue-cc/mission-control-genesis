// Historical vendor shims used before these packages shipped bundled types.
// Keep this file as an external module so tsconfig includes remain stable while
// the real declarations from node-pty and @xterm/* own their public APIs.
export {}
